const initiateMpesaPayment = (userId, amount) => {
    // We will call Mpesa API and handle the response
    console.log(`Initiating payment for User ${userId} for amount ${amount}`);
  };
  
  module.exports = { initiateMpesaPayment };  